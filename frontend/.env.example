# Optional: only if API is on another domain (e.g. api.yourdomain.com).
# Leave unset when frontend and API are same origin (default Hostinger Node deploy).
# VITE_API_BASE=https://api.yourdomain.com/api
